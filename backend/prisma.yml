#endpoint: ${env:PRISMA_ENDPOINT}
endpoint: https://eu1.prisma.sh/theran/backend/dev
# endpoint: https://grown-production-application.herokuapp.com/grown-production/prod
datamodel: datamodel.graphql
secret: ${env:PRISMA_SECRET}
hooks:
  post-deploy:
    - graphql get-schema -p prisma
